{
    "signing": {
        "default": {
            "expiry": "{{pki.signing_expiry}}"
        },
        "profiles": {
            "default": {
                "expiry": "{{pki.signing_expiry}}",
                "usages": [
                    "signing",
                    "digital signature",
                    "key encipherment",
                    "cert sign",
                    "crl sign",
                    "server auth",
                    "client auth"
                ],
                "ca_constraint": {
                    "is_ca": {{is_ca}}
                }
            }
        }
    }
}
